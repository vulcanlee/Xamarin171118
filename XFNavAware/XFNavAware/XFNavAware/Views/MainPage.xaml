<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XFNavAware.Views.MainPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Show" Command="{Binding ShowLogCommand}"/>
        <ToolbarItem Text="Reset" Command="{Binding ResetLogCommand}"/>
    </ContentPage.ToolbarItems>

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Label Text="Welcome to Xamarin Forms and Prism!" />
        <Button Text="切換 Page1"
                Command="{Binding GoPage1Command}"/>
        <Button Text="深度導航"
                Command="{Binding GoDeepCommand}"/>
    </StackLayout>

</ContentPage>